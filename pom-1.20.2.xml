<project>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>DHSupport-${mc.api-version}${mc.api-version-patch}</artifactId>

    <parent>
        <groupId>no.jckf</groupId>
        <artifactId>DHSupport</artifactId>
        <version>${revision}</version>
        <relativePath>./pom.xml</relativePath>
    </parent>

    <properties>
        <mc.api-version>1.20</mc.api-version>
        <mc.api-version-patch>.2</mc.api-version-patch>
        <dhs.read-forge-byte>true</dhs.read-forge-byte>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${mc.api-version}${mc.api-version-patch}-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
